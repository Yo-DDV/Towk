---
title: Quick Start
description: Build and run a local Towk server from the public source repository.
---

import { Aside, CardGrid, LinkCard, Steps } from "@astrojs/starlight/components";

Towk can run as one binary with an embedded NATS data store. The following path
is intended for local evaluation; use the deployment guides for an
Internet-facing server.

<Aside type="note">
  Towk does not currently publish stable binary releases or a Homebrew formula.
  Build from a reviewed source commit, or use an immutable image tag from the
  repository's Packages page once available. Do not substitute an upstream
  Chatto binary and present it as a Towk release.
</Aside>

<Steps>

1. **Clone the Towk repository**

   ```bash
   git clone https://github.com/Yo-DDV/Towk.git
   cd Towk
   ```

2. **Install the pinned development toolchain**

   Install [mise](https://mise.jdx.dev/), review `mise.toml`, then run:

   ```bash
   mise trust
   mise install
   mise run build-dev-cli
   ```

3. **Generate a local configuration**

   ```bash
   mkdir -p .local/quick-start
   cd .local/quick-start
   ../../cli/bin/chatto init
   ```

   The command creates the compatibility-named `chatto.toml` file with random
   signing, encryption, runtime-token, asset, and NATS secrets. Keep that file
   private and review it before exposing the server.

4. **Start Towk**

   ```bash
   ../../cli/bin/chatto run -c chatto.toml
   ```

5. **Open the local server**

   Visit [http://localhost:4000](http://localhost:4000). Stop the process with
   `Ctrl-C` when finished.

</Steps>

The local configuration uses embedded NATS. Uploaded files also use NATS unless
you configure S3-compatible storage. Voice and video calls require a separate
[LiveKit](https://github.com/livekit/livekit) server; they can remain disabled
for this evaluation.

## Next steps

Before a durable deployment, decide how you will handle TLS, SMTP, backups,
owner recovery, external NATS, object storage, calls, push notifications, and
monitoring.

<CardGrid>
  <LinkCard
    title="Read This First"
    href="/guides/deployment/read-this-first/"
    description="Choose a deployment path and make the important production decisions."
  />
  <LinkCard
    title="Docker Compose"
    href="/guides/deployment/docker-compose/"
    description="Run Towk, NATS, LiveKit, and Caddy on one server."
  />
  <LinkCard
    title="Standalone Binary"
    href="/guides/deployment/binary/"
    description="Operate a source-built Towk binary with embedded NATS."
  />
  <LinkCard
    title="Kubernetes"
    href="/guides/deployment/kubernetes/"
    description="Review the requirements for a scaled deployment."
  />
</CardGrid>
