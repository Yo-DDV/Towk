---
title: Media & Attachments
description: Operate Towk uploads, voice messages, image transforms, room files, link previews, S3 storage, and video processing.
---

import { Aside, CardGrid, LinkCard } from "@astrojs/starlight/components";

Towk can handle ordinary file uploads, first-class voice messages, images, videos, avatars, server branding images, link-preview images, and animated GIFs. Video transcoding is optional. Small deployments can keep assets in NATS ObjectStore; larger deployments should usually move assets to S3-compatible storage.

## What Members Can Upload

Members can attach files through drag-and-drop, paste, or the file picker when they have `message.attach` and the relevant posting permission.

On Windows, macOS, and Linux, Towk accepts every file object the browser exposes from the native clipboard. This covers Chromium's desktop implementations, Firefox's native file paste, and WebKit/Safari on macOS. Current Firefox implementations on macOS and Linux can expose only the first native file, and current non-Cocoa WebKit does not expose native clipboard files. Towk never reads a local path itself: if the clipboard contains a local file reference but the browser withholds the file, the composer consumes the reference, displays an error, and leaves the path out of the message. Drag-and-drop and the picker remain available in that case.

| Asset type | Behavior |
| ---------- | -------- |
| Images | Dimensions are inspected, image URLs can request resized WebP variants, and thumbnails are cached when enabled. |
| Documents and ordinary files | Stored as persisted assets and exposed through signed URLs. |
| Voice messages | Recorded from the composer, reviewed locally before send, stored as typed audio assets with duration and waveform metadata, then rendered with Towk's waveform player. |
| Archives | ZIP, 7z, RAR, tar, and compressed archives are accepted as opaque files and are not extracted by Towk. |
| Executables and installers | Direct executable, installer, command-script, WebAssembly, and macro-enabled Office payloads are rejected by browser metadata/content checks and authoritative server checks. |
| Videos | Stored as originals; when processing is enabled, Towk also generates a thumbnail and portable MP4 variants for cross-platform playback. |
| Animated GIFs | Uploaded as images when video processing is disabled; routed through video processing when enabled. |
| Avatars, logos, banners | Stored in the same asset system but gated by profile or admin permissions. |
| Link-preview images | Downloaded, converted to a size-limited WebP, and stored in a dedicated quota-bounded NATS object store. |

The default general upload limit is 25 MB and applies to original videos while processing is disabled. Enabling video processing gives videos a separate 100 MB default limit.

Each message is limited to 10 attachments. Upload sessions use chunks of at most 1 MiB, expire after 15 minutes while open, and unclaimed completed assets expire after 24 hours. These application bounds complement, but do not replace, the storage quota and request-rate controls of the NATS, S3, reverse-proxy, or WAF layer.

`SERVER_ASSETS` also has a hard 10 GB JetStream quota by default. Configure it with `core.assets.max_store_bytes` or `CHATTO_CORE_ASSETS_MAX_STORE_BYTES`. The limit covers NATS-backed assets and temporary upload chunks; when S3 is enabled, the final bucket still needs its own provider-side quota and monitoring. Lowering the NATS limit preserves existing objects and blocks new writes until usage is back under the limit.

The executable policy is defense in depth, not a malware scanner. Filename extensions and browser MIME types are untrusted hints; the server normalizes bounded metadata and inspects the checksum-verified payload before it creates an asset. Towk deliberately does not unpack archives, so archive-bomb expansion is outside the upload path.

## Permission And Access Model

File attachments require:

- room membership
- `message.post` or `message.post-in-thread`
- `message.attach`

First-class voice messages require room membership, `message.post` or
`message.post-in-thread`, and `message.voice`. They do not require
`message.attach` unless the same message also includes ordinary file
attachments. Towk rechecks `message.voice` when the upload starts, when the
asset is completed, and when the message is posted, so revoking the permission
cannot be bypassed by an in-progress recording.

Attachment URLs are signed per user and still check that the signed user can access the room. If a user leaves, is removed, or loses room access, new asset fetches fail even if an old message still contains the URL.

<Aside type="note">
  Signed attachment URLs are bearer capabilities until they expire. Towk keeps their TTL bounded and checks the signed user's current room access on every protected asset fetch.
</Aside>

Streamed protected originals and derivatives use a private revalidating browser
cache. Towk authorizes the ticket and current room membership before it can
return `304 Not Modified`; the Service Worker does not persist the body. Heavy
passive S3-backed originals may instead redirect after authorization to a
short-lived object URL whose response is not stored. Streamed originals also
support a single HTTP byte range for media seeking.

## Room Files

The room sidebar Files panel lists current accessible attachments from root messages and thread replies. It derives from visible message state, so files disappear when their message is deleted or the attachment is removed.

Use this as a member convenience surface, not as a permanent document-management system. There is no attachment search or media filtering yet.

## Link Previews

Towk fetches link-preview metadata when a user composes a message with a URL. New preview images use the dedicated `LINK_PREVIEW_ASSETS` NATS object store even when ordinary uploads use S3. This isolates remotely fetched content behind a hard quota. A same-ID `SERVER_ASSETS` object link lets older replicas serve new preview images during rolling upgrades and rollback without duplicating bytes; older S3 and legacy `SERVER_ASSETS` preview objects also remain readable after upgrade.

The fetcher is designed to be SSRF-safe, rejects images above 10 million decoded pixels, limits the encoded WebP to 1 MiB, coalesces concurrent requests for the same URL, and runs at most two remote fetches per process. It caches successful and failed lookups for bounded periods and applies shared source-IP and user request budgets. An elected cleanup pass waits for projection replay, preserves images referenced by current messages even when lifecycle metadata is stale or missing after restore, repairs interrupted claims, and removes expired unclaimed or no-longer-referenced objects. Operators do not need a separate preview worker.

## Storage Backend Choice

| Deployment | Recommended backend |
| ---------- | ------------------- |
| Small server with light uploads | NATS ObjectStore, the default. |
| Active community with many files | S3-compatible storage. |
| Video-heavy server | S3-compatible storage plus video processing. |
| Replicated NATS cluster | S3-compatible storage to avoid multiplying large binary storage. |

Switching to S3 affects new assets. Existing NATS-stored assets continue to work because each asset records its own backend.

## Video And Animated GIF Processing

Video processing is optional and disabled by default. Original videos remain shareable without it and use the general attachment limit. Browser playback then depends on the browser and operating system supporting the original container and codec; the original remains available as a room file. When processing is enabled, Towk requires `ffmpeg` and `ffprobe`, keeps the original upload, generates a thumbnail, and creates portable MP4 variants based on the source resolution. Processed sources are bounded by upload size, duration, and source display pixel area so a small but extremely long or oversized file cannot monopolize the transcoder.

Mobile uploads can arrive with incomplete metadata. Towk normalizes common video extensions such as `.mov`, `.m4v`, `.mp4`, and `.webm` when the browser sends an empty or generic MIME type, so phone-origin videos still enter the media pipeline. The web client mirrors that limited video-candidate policy for upload-limit feedback, local previews, and the upload metadata it sends, while the server remains the authoritative validator. When video processing is enabled and no processing manifest exists yet, the client waits for the processed portable variant for every user video source instead of relying on the raw original. Browser labels such as MP4 or WebM are containers, not a cross-platform codec guarantee.

When video processing is disabled, original videos and GIFs still remain available as attachments, but cross-platform playback is limited to what the viewer's browser and operating system can decode natively.

Voice messages use the same room-scoped asset pipeline but are not controlled by
`CHATTO_VIDEO_ENABLED`. Browsers record different audio containers, so Towk
accepts WebM/Opus, MP4/AAC, and Ogg/Opus voice uploads, validates the declared
container, probes the uploaded input with `ffprobe`, rejects containers that
include a video track, and normalizes non-MP4 recordings into M4A/AAC before
making the asset durable. That gives iOS, Android, Linux, Firefox, Safari,
Chrome, and Chromium-based PWAs one playback-oriented audio format while
preventing voice messages from becoming a bypass for video uploads. Non-MP4
normalization uses `ffmpeg`; voice probes and normalization both share the
media-transcode worker pool. MP4 voice uploads can complete without transcoding
after the input probe passes.

Ordinary audio attachments are different from first-class voice messages: Towk
stores them as user-provided files, renders the browser's native audio player
when possible, and always keeps a download action visible. Native playback then
depends on the viewer's operating system and browser codec support.

Enable it only when the server has CPU and temporary disk space for transcoding:

```toml
[performance]
default_profile = "balanced"
max_video_workers = 2
[video]
enabled = true
```

The official Docker image includes the required ffmpeg tools. Host installs need
those binaries installed separately for video/GIF processing and for
cross-platform voice-message normalization. Tune throughput with the runtime
performance profile and `performance.max_video_workers`; keep
`video.max_concurrent` only for compatibility with older configuration files.

<Aside type="caution">
  Video processing is currently in-process and best-effort. Towk records durable asset processing state and retries missed unmanifested video assets on boot, but operators should still treat transcoding as CPU-heavy background work.
</Aside>

## Active Document Safety

Uploaded HTML, XHTML, SVG, XML, and XML-derived active document formats are served with a sandbox policy and `nosniff`. S3-backed active documents stream through Towk instead of redirecting directly to object storage so the sandbox headers stay in place.

## Related Guides

<CardGrid>
  <LinkCard title="S3 Storage" href="/guides/infrastructure/s3-storage/" description="Configure S3-compatible storage for persisted assets." />
  <LinkCard title="Video Processing" href="/guides/infrastructure/video-processing/" description="Enable ffmpeg-backed video and animated-GIF processing." />
  <LinkCard title="Asset Upload API" href="/reference/connectrpc-api/asset-uploads/" description="Typed API reference for uploads, including voice-message metadata." />
  <LinkCard title="Backup & Restore" href="/guides/operations/backup-restore/" description="Plan backups that include asset storage and encryption keys." />
  <LinkCard title="Room Attachment API" href="/reference/connectrpc-api/rooms/#chatto-api-v1-RoomService-ListRoomAttachments" description="Typed API reference for room attachment list reads." />
</CardGrid>
