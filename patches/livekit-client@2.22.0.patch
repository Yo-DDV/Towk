diff --git a/dist/livekit-client.esm.mjs b/dist/livekit-client.esm.mjs
index 731beefaf7f814aadc346f0294208bb84a2683b8..8a71362d2c69da26bcb7d589f66157bac911bf0c 100644
--- a/dist/livekit-client.esm.mjs
+++ b/dist/livekit-client.esm.mjs
@@ -18719,13 +18719,34 @@ class PCTransport extends eventsExports.EventEmitter {
             });
           }
         });
-        // The server's answer sets per-track codec params (e.g. opus `usedtx`) on
-        // the sections mapped to a published track, but leaves the pre-populated
-        // recvonly placeholder sections with defaults. Conform the placeholders so
-        // each shared payload type is consistent across the bundle, otherwise
-        // libwebrtc flags a "bundled payload type collision".
+        // x-google-start-bitrate is a local sender hint. Remove an SFU echo
+        // consistently before applying a bundled remote answer.
+        for (const media of sdpParsed.media) {
+          if (media.type !== 'video') continue;
+          media.fmtp = media.fmtp.flatMap(fmtp => {
+            const config = fmtp.config.split(';').filter(parameter => !parameter.startsWith('x-google-start-bitrate=')).join(';');
+            return config ? [{
+              ...fmtp,
+              config
+            }] : [];
+          });
+        }
+        // Chromium rejects a BUNDLE answer when an SFU reintroduces `stereo=1`
+        // on recv-only sections. Other engines retain the server's SDP.
+        const publishedAudioMids = new Set(this.trackBitrates.flatMap(trackbr => {
+          var _trackbr$transceiver;
+          const mid = (_trackbr$transceiver = trackbr.transceiver) === null || _trackbr$transceiver === void 0 ? void 0 : _trackbr$transceiver.mid;
+          if (!mid) return [];
+          const media = sdpParsed.media.find(candidate => candidate.type === 'audio' && getMidString(candidate.mid) === mid);
+          return media ? [mid] : [];
+        }));
+        if (isChromiumBased() && publishedAudioMids.size > 0) {
+          conformBundledCodecFmtp(sdpParsed.media, media => media.type === 'audio' && !publishedAudioMids.has(getMidString(media.mid)));
+        }
+        // Chromium requires the recv-only placeholders to carry the same codec
+        // parameters as published sections. Other engines retain the server answer.
         const placeholderMids = this.getPlaceholderMids();
-        if (placeholderMids.size > 0) {
+        if (isChromiumBased() && placeholderMids.size > 0) {
           conformBundledCodecFmtp(sdpParsed.media, media => placeholderMids.has(getMidString(media.mid)));
         }
         mungedSDP = libExports.write(sdpParsed);
@@ -18858,13 +18879,11 @@ class PCTransport extends eventsExports.EventEmitter {
             });
           }
         });
-        // Conform the placeholder sections (pre-populated, or reverted after an
-        // unpublish) so every shared payload type carries identical fmtp across the
-        // bundle, otherwise libwebrtc flags a "bundled payload type collision".
-        // Detection is by transceiver (mids are stable across renegotiations) since
-        // an unpublished section keeps its `a=msid`.
+        // Chromium requires placeholder sections to carry identical fmtp across
+        // the bundle. Other engines retain their native offer. Detection is by
+        // transceiver because mids remain stable across renegotiations.
         const placeholderMids = this.getPlaceholderMids();
-        if (placeholderMids.size > 0) {
+        if (isChromiumBased() && placeholderMids.size > 0) {
           conformBundledCodecFmtp(sdpParsed.media, media => placeholderMids.has(getMidString(media.mid)));
         }
         if (this.latestOfferId > offerId) {
diff --git a/src/room/PCTransport.ts b/src/room/PCTransport.ts
index 5908ff1b0f319e4137f1c5eedd9ec1fe300d3069..7b47c2f9d009102a0860ad4e46f6fb2b42440205 100644
--- a/src/room/PCTransport.ts
+++ b/src/room/PCTransport.ts
@@ -7,7 +7,7 @@ import log, { LoggerNames, getLogger } from '../logger';
 import { debounce } from './debounce';
 import { NegotiationError, UnexpectedConnectionState } from './errors';
 import type { LoggerOptions } from './types';
-import { ddExtensionURI, isSVCCodec, isSafari } from './utils';
+import { ddExtensionURI, isChromiumBased, isSVCCodec, isSafari } from './utils';
 
 /** @internal */
 interface TrackBitrateInfo {
@@ -317,13 +317,43 @@ export default class PCTransport extends (EventEmitter as new () => TypedEmitter
           });
         }
       });
-      // The server's answer sets per-track codec params (e.g. opus `usedtx`) on
-      // the sections mapped to a published track, but leaves the pre-populated
-      // recvonly placeholder sections with defaults. Conform the placeholders so
-      // each shared payload type is consistent across the bundle, otherwise
-      // libwebrtc flags a "bundled payload type collision".
+      // x-google-start-bitrate is a local sender hint. Some SFU answers echo it
+      // only on published sections, which makes the answer internally invalid
+      // for a BUNDLE group. The local offer has already configured the sender,
+      // so remove the hint consistently before applying the remote answer.
+      for (const media of sdpParsed.media) {
+        if (media.type !== 'video') continue;
+        media.fmtp = media.fmtp.flatMap((fmtp) => {
+          const config = fmtp.config
+            .split(';')
+            .filter((parameter) => !parameter.startsWith('x-google-start-bitrate='))
+            .join(';');
+          return config ? [{ ...fmtp, config }] : [];
+        });
+      }
+      // Chromium rejects a BUNDLE answer when an SFU reintroduces `stereo=1`
+      // on recv-only sections. Other engines receive the server SDP unchanged.
+      const publishedAudioMids = new Set(
+        this.trackBitrates.flatMap((trackbr) => {
+          const mid = trackbr.transceiver?.mid;
+          if (!mid) return [];
+          const media = sdpParsed.media.find(
+            (candidate) => candidate.type === 'audio' && getMidString(candidate.mid!) === mid,
+          );
+          return media ? [mid] : [];
+        }),
+      );
+      if (isChromiumBased() && publishedAudioMids.size > 0) {
+        conformBundledCodecFmtp(
+          sdpParsed.media,
+          (media) =>
+            media.type === 'audio' && !publishedAudioMids.has(getMidString(media.mid!)),
+        );
+      }
+      // Chromium requires the recv-only placeholders to carry the same codec
+      // parameters as published sections. Other engines retain the server answer.
       const placeholderMids = this.getPlaceholderMids();
-      if (placeholderMids.size > 0) {
+      if (isChromiumBased() && placeholderMids.size > 0) {
         conformBundledCodecFmtp(sdpParsed.media, (media) =>
           placeholderMids.has(getMidString(media.mid!)),
         );
@@ -482,13 +512,11 @@ export default class PCTransport extends (EventEmitter as new () => TypedEmitter
           });
         }
       });
-      // Conform the placeholder sections (pre-populated, or reverted after an
-      // unpublish) so every shared payload type carries identical fmtp across the
-      // bundle, otherwise libwebrtc flags a "bundled payload type collision".
-      // Detection is by transceiver (mids are stable across renegotiations) since
-      // an unpublished section keeps its `a=msid`.
+      // Chromium requires placeholder sections to carry identical fmtp across
+      // the bundle. Other engines retain their native offer. Detection is by
+      // transceiver because mids remain stable across renegotiations.
       const placeholderMids = this.getPlaceholderMids();
-      if (placeholderMids.size > 0) {
+      if (isChromiumBased() && placeholderMids.size > 0) {
         conformBundledCodecFmtp(sdpParsed.media, (media) =>
           placeholderMids.has(getMidString(media.mid!)),
         );
