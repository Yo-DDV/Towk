const WINDOW_DAYS = 30;

export const ACTIVITY_LOCALES = {
  en: {
    numberLocale: "en-US", dateLocale: "en-GB",
    ariaTitle: "Towk repository activity over the last 30 UTC days",
    ariaDescription: "Daily commits, merged pull requests, closed issues, code movement and pull request flow for the default branch.",
    context: "MAIN · LAST 30 UTC DAYS · UTC", refreshed: "Refreshed",
    commits: "Commits", commitsScope: "default branch",
    mergedPullRequests: "Merged PRs", mergedPullRequestsScope: "merged into main",
    issuesClosed: "Issues closed", issuesClosedScope: "pull requests excluded",
    activeContributors: "Active contributors", activeContributorsScope: "human commit / PR authors",
    medianLeadTime: "Median merge lead time", medianFirstReview: "Median first review",
    dailyDelivery: "Daily delivery", dailyDeliverySubtitle: "Commits and merged pull requests",
    issueThroughput: "Issue throughput", issueThroughputSubtitle: "Issues closed each UTC day",
    codeMovement: "Code movement", codeMovementSubtitle: "Added and deleted lines on main",
    pullRequestFlow: "Pull request flow", pullRequestFlowSubtitle: "Median lead time by merge day",
    additions: "Additions", deletions: "Deletions", filesChanged: "files changed",
    partialDay: "Current UTC day is partial",
    footer: "Generated by GitHub Actions · totals include automation · contributors exclude bots",
    dayUnit: "d"
  },
  fr: {
    numberLocale: "fr-FR", dateLocale: "fr-FR",
    ariaTitle: "Activité du dépôt Towk sur les 30 derniers jours UTC",
    ariaDescription: "Commits quotidiens, pull requests fusionnées, issues fermées, mouvements de code et flux des pull requests sur la branche principale.",
    context: "MAIN · 30 DERNIERS JOURS · UTC", refreshed: "Actualisé",
    commits: "Commits", commitsScope: "branche principale",
    mergedPullRequests: "PR fusionnées", mergedPullRequestsScope: "fusionnées dans main",
    issuesClosed: "Issues fermées", issuesClosedScope: "pull requests exclues",
    activeContributors: "Contributeurs actifs", activeContributorsScope: "auteurs humains de commits / PR",
    medianLeadTime: "Délai médian de fusion", medianFirstReview: "Première revue médiane",
    dailyDelivery: "Livraison quotidienne", dailyDeliverySubtitle: "Commits et pull requests fusionnées",
    issueThroughput: "Traitement des issues", issueThroughputSubtitle: "Issues fermées par jour UTC",
    codeMovement: "Mouvement du code", codeMovementSubtitle: "Lignes ajoutées et supprimées sur main",
    pullRequestFlow: "Flux des pull requests", pullRequestFlowSubtitle: "Délai médian par jour de fusion",
    additions: "Ajouts", deletions: "Suppressions", filesChanged: "fichiers modifiés",
    partialDay: "Le jour UTC courant est partiel",
    footer: "Généré par GitHub Actions · totaux avec automatisation · robots exclus des contributeurs",
    dayUnit: "j"
  },
  de: {
    numberLocale: "de-DE", dateLocale: "de-DE",
    ariaTitle: "Towk-Repository-Aktivität der letzten 30 UTC-Tage",
    ariaDescription: "Tägliche Commits, zusammengeführte Pull Requests, geschlossene Issues, Codebewegung und Pull-Request-Durchlauf auf dem Standard-Branch.",
    context: "MAIN · LETZTE 30 TAGE · UTC", refreshed: "Aktualisiert",
    commits: "Commits", commitsScope: "Standard-Branch",
    mergedPullRequests: "Zusammengeführte PRs", mergedPullRequestsScope: "in main zusammengeführt",
    issuesClosed: "Geschlossene Issues", issuesClosedScope: "Pull Requests ausgeschlossen",
    activeContributors: "Aktive Mitwirkende", activeContributorsScope: "menschliche Commit-/PR-Autoren",
    medianLeadTime: "Median bis zur Zusammenführung", medianFirstReview: "Median bis zur ersten Review",
    dailyDelivery: "Tägliche Lieferung", dailyDeliverySubtitle: "Commits und zusammengeführte Pull Requests",
    issueThroughput: "Issue-Durchsatz", issueThroughputSubtitle: "Pro UTC-Tag geschlossene Issues",
    codeMovement: "Codebewegung", codeMovementSubtitle: "Hinzugefügte und gelöschte Zeilen auf main",
    pullRequestFlow: "Pull-Request-Durchlauf", pullRequestFlowSubtitle: "Median nach Zusammenführungstag",
    additions: "Hinzugefügt", deletions: "Gelöscht", filesChanged: "geänderte Dateien",
    partialDay: "Der aktuelle UTC-Tag ist unvollständig",
    footer: "Von GitHub Actions erzeugt · Summen enthalten Automation · Bots zählen nicht als Mitwirkende",
    dayUnit: "T"
  },
  es: {
    numberLocale: "es-ES", dateLocale: "es-ES",
    ariaTitle: "Actividad del repositorio Towk durante los últimos 30 días UTC",
    ariaDescription: "Commits diarios, pull requests fusionadas, issues cerradas, movimiento de código y flujo de pull requests en la rama predeterminada.",
    context: "MAIN · ÚLTIMOS 30 DÍAS · UTC", refreshed: "Actualizado",
    commits: "Commits", commitsScope: "rama predeterminada",
    mergedPullRequests: "PR fusionadas", mergedPullRequestsScope: "fusionadas en main",
    issuesClosed: "Issues cerradas", issuesClosedScope: "pull requests excluidas",
    activeContributors: "Colaboradores activos", activeContributorsScope: "autores humanos de commits / PR",
    medianLeadTime: "Mediana hasta la fusión", medianFirstReview: "Mediana hasta la primera revisión",
    dailyDelivery: "Entrega diaria", dailyDeliverySubtitle: "Commits y pull requests fusionadas",
    issueThroughput: "Ritmo de issues", issueThroughputSubtitle: "Issues cerradas por día UTC",
    codeMovement: "Movimiento de código", codeMovementSubtitle: "Líneas añadidas y eliminadas en main",
    pullRequestFlow: "Flujo de pull requests", pullRequestFlowSubtitle: "Mediana por día de fusión",
    additions: "Adiciones", deletions: "Eliminaciones", filesChanged: "archivos modificados",
    partialDay: "El día UTC actual está incompleto",
    footer: "Generado por GitHub Actions · los totales incluyen automatización · los bots no cuentan como colaboradores",
    dayUnit: "d"
  },
  pt: {
    numberLocale: "pt-PT", dateLocale: "pt-PT",
    ariaTitle: "Atividade do repositório Towk nos últimos 30 dias UTC",
    ariaDescription: "Commits diários, pull requests integradas, issues fechadas, movimento de código e fluxo de pull requests no ramo predefinido.",
    context: "MAIN · ÚLTIMOS 30 DIAS · UTC", refreshed: "Atualizado",
    commits: "Commits", commitsScope: "ramo predefinido",
    mergedPullRequests: "PR integradas", mergedPullRequestsScope: "integradas em main",
    issuesClosed: "Issues fechadas", issuesClosedScope: "pull requests excluídas",
    activeContributors: "Colaboradores ativos", activeContributorsScope: "autores humanos de commits / PR",
    medianLeadTime: "Mediana até à integração", medianFirstReview: "Mediana até à primeira revisão",
    dailyDelivery: "Entrega diária", dailyDeliverySubtitle: "Commits e pull requests integradas",
    issueThroughput: "Ritmo das issues", issueThroughputSubtitle: "Issues fechadas por dia UTC",
    codeMovement: "Movimento do código", codeMovementSubtitle: "Linhas adicionadas e removidas em main",
    pullRequestFlow: "Fluxo das pull requests", pullRequestFlowSubtitle: "Mediana por dia de integração",
    additions: "Adições", deletions: "Remoções", filesChanged: "ficheiros modificados",
    partialDay: "O dia UTC atual está incompleto",
    footer: "Gerado pelo GitHub Actions · os totais incluem automação · bots não contam como colaboradores",
    dayUnit: "d"
  }
};

export function escapeXml(value) {
  return String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&apos;");
}

function validDate(value) {
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? null : date;
}
function utcDay(date) { return new Date(Date.UTC(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate())); }
function addUtcDays(date, amount) { const next = new Date(date); next.setUTCDate(next.getUTCDate() + amount); return next; }
function dayKey(date) { return date.toISOString().slice(0, 10); }
function dateFromCommit(commit) { return validDate(commit?.commit?.committer?.date ?? commit?.commit?.author?.date); }
function dateFromPullRequest(pr) { return validDate(pr?.merged_at); }
function dateFromIssue(issue) { return validDate(issue?.closed_at); }

export function isBotIdentity(identity) {
  if (!identity) return false;
  const login = String(identity.login ?? identity.name ?? "");
  return identity.type === "Bot" || /\[bot\]$/i.test(login) || /^(?:github-actions|dependabot)(?:\[bot\])?$/i.test(login);
}
function commitIdentity(commit) {
  if (commit?.author?.login) return { key: commit.author.login.toLowerCase(), bot: isBotIdentity(commit.author) };
  const name = String(commit?.commit?.author?.name ?? "").trim();
  return name ? { key: name.toLowerCase(), bot: /(?:^|\W)bot(?:\W|$)/i.test(name) } : null;
}
function prIdentity(pr) {
  if (!pr?.user?.login) return null;
  return { key: pr.user.login.toLowerCase(), bot: isBotIdentity(pr.user) };
}

export function median(values) {
  const numbers = values.map(Number).filter(Number.isFinite).sort((a, b) => a - b);
  if (!numbers.length) return null;
  const middle = Math.floor(numbers.length / 2);
  return numbers.length % 2 ? numbers[middle] : (numbers[middle - 1] + numbers[middle]) / 2;
}
function hoursBetween(start, end) {
  const from = validDate(start); const to = validDate(end);
  return !from || !to || to < from ? null : (to - from) / 3_600_000;
}
function firstHumanReviewHours(pr) {
  const author = String(pr?.user?.login ?? "").toLowerCase();
  const created = validDate(pr?.created_at); const merged = validDate(pr?.merged_at);
  if (!created) return null;
  const first = (pr?.reviews ?? [])
    .map((review) => ({ review, at: validDate(review?.submitted_at) }))
    .filter(({ review, at }) => {
      const login = String(review?.user?.login ?? "").toLowerCase();
      return at && login && login !== author && !isBotIdentity(review.user) && at >= created && (!merged || at <= merged);
    })
    .sort((a, b) => a.at - b.at)[0];
  return first ? hoursBetween(created, first.at) : null;
}
function makeDays(now) {
  const end = utcDay(now);
  return Array.from({ length: WINDOW_DAYS }, (_, index) => {
    const date = addUtcDays(end, index - WINDOW_DAYS + 1);
    return { date: dayKey(date), label: String(date.getUTCDate()), commits: 0, mergedPullRequests: 0,
      issuesClosed: 0, additions: 0, deletions: 0, filesChanged: 0, leadTimes: [] };
  });
}
function statFor(commitStats, sha) {
  const value = commitStats instanceof Map ? commitStats.get(sha) : commitStats?.[sha];
  return { additions: Math.max(0, Number(value?.additions) || 0), deletions: Math.max(0, Number(value?.deletions) || 0), filesChanged: Math.max(0, Number(value?.filesChanged) || 0) };
}

export function buildRepositoryActivity({ commits = [], pullRequests = [], issues = [], commitStats = {}, now = new Date() } = {}) {
  const snapshot = validDate(now);
  if (!snapshot) throw new Error("Invalid repository-activity snapshot date");
  const start = addUtcDays(utcDay(snapshot), 1 - WINDOW_DAYS);
  const inWindow = (date) => date && date >= start && date <= snapshot;
  const selectedCommits = commits.filter((item) => inWindow(dateFromCommit(item)));
  const selectedPrs = pullRequests.filter((item) => inWindow(dateFromPullRequest(item)));
  const selectedIssues = issues.filter((item) => !item?.pull_request && inWindow(dateFromIssue(item)));
  const days = makeDays(snapshot); const byDate = new Map(days.map((day) => [day.date, day]));
  const humans = new Set(); const leadTimes = []; const firstReviews = [];
  let additions = 0; let deletions = 0; let filesChanged = 0; let botCommits = 0; let botPrs = 0;

  for (const commit of selectedCommits) {
    const bucket = byDate.get(dayKey(dateFromCommit(commit))); if (!bucket) continue;
    bucket.commits += 1; const stats = statFor(commitStats, commit.sha);
    for (const key of ["additions", "deletions", "filesChanged"]) bucket[key] += stats[key];
    additions += stats.additions; deletions += stats.deletions; filesChanged += stats.filesChanged;
    const identity = commitIdentity(commit); if (identity?.bot) botCommits += 1; else if (identity) humans.add(identity.key);
  }
  for (const pr of selectedPrs) {
    const bucket = byDate.get(dayKey(dateFromPullRequest(pr))); if (!bucket) continue;
    bucket.mergedPullRequests += 1;
    const lead = hoursBetween(pr.created_at, pr.merged_at); if (lead !== null) { leadTimes.push(lead); bucket.leadTimes.push(lead); }
    const first = firstHumanReviewHours(pr); if (first !== null) firstReviews.push(first);
    const identity = prIdentity(pr); if (identity?.bot) botPrs += 1; else if (identity) humans.add(identity.key);
  }
  for (const issue of selectedIssues) { const bucket = byDate.get(dayKey(dateFromIssue(issue))); if (bucket) bucket.issuesClosed += 1; }
  const daily = days.map(({ leadTimes: values, ...day }) => ({ ...day, medianPrLeadTimeHours: median(values) }));
  return {
    version: 3, generatedAt: snapshot.toISOString(),
    window: { kind: "rolling-30-utc-days", days: WINDOW_DAYS, start: start.toISOString(), end: snapshot.toISOString(), currentDayPartial: true },
    headline: { commits: selectedCommits.length, mergedPullRequests: selectedPrs.length, issuesClosed: selectedIssues.length, activeHumanContributors: humans.size },
    flow: { medianPrLeadTimeHours: median(leadTimes), medianFirstReviewHours: median(firstReviews), reviewedPullRequests: firstReviews.length },
    codeMovement: { additions, deletions, filesChanged },
    automation: { commits: botCommits, mergedPullRequests: botPrs }, daily
  };
}

function fmtNumber(value, copy, digits = 0) { return new Intl.NumberFormat(copy.numberLocale, { maximumFractionDigits: digits }).format(Math.max(0, Number(value) || 0)); }
function fmtDuration(hours, copy) {
  if (!Number.isFinite(hours)) return "—";
  if (hours < 1) return "<1 h";
  if (hours < 48) return `${fmtNumber(hours, copy, hours < 10 ? 1 : 0)} h`;
  return `${fmtNumber(hours / 24, copy, 1)} ${copy.dayUnit}`;
}
function fmtGenerated(value, copy) { return new Intl.DateTimeFormat(copy.dateLocale, { dateStyle: "medium", timeStyle: "short", timeZone: "UTC" }).format(new Date(value)); }

function styles() { return `<style>
.bg{fill:#fff;stroke:#d0d7de}.surface{fill:#f6f8fa;stroke:#d8dee4}.strong{fill:#eef7ff;stroke:#b6d8ff}.ink{fill:#162033}.muted{fill:#667085}.faint{fill:#98a2b3}.grid{stroke:#dfe5ec}.track{fill:#e7edf3}
.meta{font:750 11px -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;letter-spacing:1.1px}.value{font:820 31px -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.label{font:720 13px -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.scope{font:520 10px -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.chart-title{font:760 14px -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.chart-subtitle{font:520 11px -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.small{font:540 10px -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.flow-value{font:780 17px -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}
@media(prefers-color-scheme:dark){.bg{fill:#0d1117;stroke:#30363d}.surface{fill:#161b22;stroke:#30363d}.strong{fill:#10263a;stroke:#244d70}.ink{fill:#f0f6fc}.muted{fill:#a7b0bc}.faint{fill:#7d8590}.grid{stroke:#30363d}.track{fill:#27313d}}
</style>`; }
function shell(width, height, copy, body) { return `<svg xmlns="http://www.w3.org/2000/svg" width="${width}" height="${height}" viewBox="0 0 ${width} ${height}" role="img" aria-labelledby="title desc">
<title id="title">${escapeXml(copy.ariaTitle)}</title><desc id="desc">${escapeXml(copy.ariaDescription)}</desc>
<defs><linearGradient id="accent" x1="0" y1="0" x2="1" y2="0"><stop offset="0%" stop-color="#43d8b0"/><stop offset="50%" stop-color="#4aa8ff"/><stop offset="100%" stop-color="#7867f2"/></linearGradient><linearGradient id="soft" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#43d8b0" stop-opacity=".18"/><stop offset="100%" stop-color="#7867f2" stop-opacity=".12"/></linearGradient></defs>${styles()}<rect class="bg" x="1" y="1" width="${width - 2}" height="${height - 2}" rx="22"/>${body}</svg>\n`; }
function grid(x, y, w, h, count = 3) { return Array.from({ length: count + 1 }, (_, i) => `<line class="grid" x1="${x}" y1="${(y + h * i / count).toFixed(1)}" x2="${x + w}" y2="${(y + h * i / count).toFixed(1)}"/>`).join(""); }
function dateLabels(data, x, y, w, every = 5) { const step = w / (data.length - 1); return data.map((item, i) => (i === 0 || i === data.length - 1 || i % every === 0) ? `<text class="small faint" x="${(x + i * step).toFixed(1)}" y="${y}" text-anchor="middle">${item.label}</text>` : "").join(""); }
function groupedBars(data, x, y, w, h, copy) { const max = Math.max(...data.flatMap((d) => [d.commits, d.mergedPullRequests]), 1); const slot = w / data.length; const bw = Math.max(2.4, Math.min(7, slot * .31)); return grid(x,y,w,h)+data.map((d,i)=>{const c=x+slot*i+slot/2; const bar=(v,o,color,label)=>{const bh=v?Math.max(3,v/max*h):1.5;return `<rect x="${(c+o).toFixed(1)}" y="${(y+h-bh).toFixed(1)}" width="${bw.toFixed(1)}" height="${bh.toFixed(1)}" rx="2" fill="${color}"><title>${d.date} · ${escapeXml(label)}: ${v}</title></rect>`};return bar(d.commits,-bw-1,"#43d8b0",copy.commits)+bar(d.mergedPullRequests,1,"#4aa8ff",copy.mergedPullRequests)}).join(""); }
function simpleBars(data, x, y, w, h, accessor, color, label) { const values=data.map(accessor); const max=Math.max(...values,1); const slot=w/data.length; const bw=Math.max(3,Math.min(10,slot*.58)); return grid(x,y,w,h)+data.map((d,i)=>{const v=accessor(d),bh=v?Math.max(3,v/max*h):1.5,xx=x+slot*i+(slot-bw)/2;return `<g><rect class="track" x="${xx.toFixed(1)}" y="${y}" width="${bw.toFixed(1)}" height="${h}" rx="3" opacity=".3"/><rect x="${xx.toFixed(1)}" y="${(y+h-bh).toFixed(1)}" width="${bw.toFixed(1)}" height="${bh.toFixed(1)}" rx="3" fill="${color}"><title>${d.date} · ${escapeXml(label)}: ${v}</title></rect></g>`}).join(""); }
function churn(data,x,y,w,h,copy){const half=h/2,max=Math.max(...data.flatMap((d)=>[d.additions,d.deletions]),1),slot=w/data.length,bw=Math.max(3,Math.min(10,slot*.58)),base=y+half;return grid(x,y,w,h,4)+`<line class="grid" x1="${x}" y1="${base}" x2="${x+w}" y2="${base}" stroke-width="1.5"/>`+data.map((d,i)=>{const xx=x+slot*i+(slot-bw)/2,a=d.additions?Math.max(2,d.additions/max*(half-4)):0,z=d.deletions?Math.max(2,d.deletions/max*(half-4)):0;return `<rect x="${xx.toFixed(1)}" y="${(base-a).toFixed(1)}" width="${bw.toFixed(1)}" height="${a.toFixed(1)}" rx="2" fill="#43d8b0"><title>${d.date} · ${escapeXml(copy.additions)}: ${d.additions}</title></rect><rect x="${xx.toFixed(1)}" y="${base}" width="${bw.toFixed(1)}" height="${z.toFixed(1)}" rx="2" fill="#7867f2"><title>${d.date} · ${escapeXml(copy.deletions)}: ${d.deletions}</title></rect>`}).join("");}
function leadBars(data,x,y,w,h,copy,overall){const vals=data.map((d)=>d.medianPrLeadTimeHours).filter(Number.isFinite),max=Math.max(...vals,Number(overall)||0,1),slot=w/data.length,bw=Math.max(3,Math.min(10,slot*.58));let line="";if(Number.isFinite(overall)){const yy=y+h-overall/max*h;line=`<line x1="${x}" y1="${yy.toFixed(1)}" x2="${x+w}" y2="${yy.toFixed(1)}" stroke="#f4a261" stroke-dasharray="5 5"/>`;}return grid(x,y,w,h)+line+data.map((d,i)=>{const v=d.medianPrLeadTimeHours,xx=x+slot*i+(slot-bw)/2;if(!Number.isFinite(v))return `<rect class="track" x="${xx.toFixed(1)}" y="${y+h-1.5}" width="${bw.toFixed(1)}" height="1.5"/>`;const bh=Math.max(3,v/max*h);return `<rect x="${xx.toFixed(1)}" y="${(y+h-bh).toFixed(1)}" width="${bw.toFixed(1)}" height="${bh.toFixed(1)}" rx="3" fill="#4aa8ff"><title>${d.date} · ${escapeXml(copy.medianLeadTime)}: ${escapeXml(fmtDuration(v,copy))}</title></rect>`}).join("");}
function header(title, subtitle, x, y, right="") { return `<text class="chart-title ink" x="${x}" y="${y}">${escapeXml(title)}</text><text class="chart-subtitle muted" x="${x}" y="${y+20}">${escapeXml(subtitle)}</text>${right}`; }
function kpis(metrics,copy,mobile){const items=[[metrics.headline.commits,copy.commits,copy.commitsScope,"#43d8b0"],[metrics.headline.mergedPullRequests,copy.mergedPullRequests,copy.mergedPullRequestsScope,"#4aa8ff"],[metrics.headline.issuesClosed,copy.issuesClosed,copy.issuesClosedScope,"#7867f2"],[metrics.headline.activeHumanContributors,copy.activeContributors,copy.activeContributorsScope,"#f4a261"]];return items.map(([v,l,s,c],i)=>{const x=mobile?20+(i%2)*195:28+i*289,y=mobile?72+Math.floor(i/2)*96:52,w=mobile?185:277,h=mobile?86:100;return `<g><rect class="surface" x="${x}" y="${y}" width="${w}" height="${h}" rx="17"/><rect x="${x}" y="${y}" width="${mobile?5:6}" height="${h}" rx="3" fill="${c}"/><text class="value ink" x="${x+(mobile?17:22)}" y="${y+(mobile?36:40)}" style="font-size:${mobile?27:31}px">${fmtNumber(v,copy)}</text><text class="label ink" x="${x+(mobile?17:22)}" y="${y+(mobile?57:65)}" style="font-size:${mobile?11.2:13}px">${escapeXml(l)}</text><text class="scope muted" x="${x+(mobile?17:22)}" y="${y+(mobile?74:85)}" style="font-size:${mobile?8.6:10}px">${escapeXml(s)}</text></g>`}).join("");}

function desktop(metrics,copy){const lx=28,rx=608,cw=564,ty=236,by=522,ch=270,px=22,pw=520,py=76,ph=145;const flow=`<g><rect class="strong" x="28" y="168" width="1144" height="52" rx="16" fill="url(#soft)"/><text class="label muted" x="50" y="190">${escapeXml(copy.medianLeadTime)}</text><text class="flow-value ink" x="50" y="210">${escapeXml(fmtDuration(metrics.flow.medianPrLeadTimeHours,copy))}</text><line class="grid" x1="300" y1="180" x2="300" y2="208"/><text class="label muted" x="326" y="190">${escapeXml(copy.medianFirstReview)}</text><text class="flow-value ink" x="326" y="210">${escapeXml(fmtDuration(metrics.flow.medianFirstReviewHours,copy))}</text><text class="scope muted" x="1148" y="200" text-anchor="end">${escapeXml(copy.partialDay)}</text></g>`;const card=(x,y,title,sub,chart,right="")=>`<g><rect class="surface" x="${x}" y="${y}" width="${cw}" height="${ch}" rx="19"/>${header(title,sub,x+22,y+34,right)}${chart}${dateLabels(metrics.daily,x+px,y+246,pw)}</g>`;return shell(1200,850,copy,`<rect x="0" y="0" width="1200" height="8" rx="4" fill="url(#accent)"/><text class="meta" x="28" y="34" fill="#4aa8ff">${escapeXml(copy.context)}</text><text class="small faint" x="1172" y="34" text-anchor="end">${escapeXml(copy.refreshed)} ${escapeXml(fmtGenerated(metrics.generatedAt,copy))}</text>${kpis(metrics,copy,false)}${flow}${card(lx,ty,copy.dailyDelivery,copy.dailyDeliverySubtitle,groupedBars(metrics.daily,lx+px,ty+py,pw,ph,copy))}${card(rx,ty,copy.issueThroughput,copy.issueThroughputSubtitle,simpleBars(metrics.daily,rx+px,ty+py,pw,ph,(d)=>d.issuesClosed,"#7867f2",copy.issuesClosed))}${card(lx,by,copy.codeMovement,copy.codeMovementSubtitle,churn(metrics.daily,lx+px,by+py,pw,ph,copy),`<text class="small muted" x="${lx+542}" y="${by+34}" text-anchor="end">+${fmtNumber(metrics.codeMovement.additions,copy)} · −${fmtNumber(metrics.codeMovement.deletions,copy)} · ${fmtNumber(metrics.codeMovement.filesChanged,copy)} ${escapeXml(copy.filesChanged)}</text>`)}${card(rx,by,copy.pullRequestFlow,copy.pullRequestFlowSubtitle,leadBars(metrics.daily,rx+px,by+py,pw,ph,copy,metrics.flow.medianPrLeadTimeHours),`<text class="small muted" x="${rx+542}" y="${by+34}" text-anchor="end">${escapeXml(fmtDuration(metrics.flow.medianPrLeadTimeHours,copy))}</text>`)}<text class="small faint" x="28" y="831">${escapeXml(copy.footer)}</text>`);}
function mobile(metrics,copy){
  const w=420,h=1572,cx=38,cw=344,ph=130,ys=[430,702,974,1246];
  const footer=copy.footer.split(" · ").map((line,index)=>`<text class="small faint" x="20" y="${1524+index*15}" style="font-size:8.8px">${escapeXml(line)}</text>`).join("");
  const flow=`<g><rect class="strong" x="20" y="274" width="380" height="138" rx="16" fill="url(#soft)"/>
    <text class="label muted" x="38" y="299" style="font-size:10.5px">${escapeXml(copy.medianLeadTime)}</text>
    <text class="flow-value ink" x="38" y="322">${escapeXml(fmtDuration(metrics.flow.medianPrLeadTimeHours,copy))}</text>
    <line class="grid" x1="38" y1="336" x2="382" y2="336"/>
    <text class="label muted" x="38" y="359" style="font-size:10.5px">${escapeXml(copy.medianFirstReview)}</text>
    <text class="flow-value ink" x="38" y="382">${escapeXml(fmtDuration(metrics.flow.medianFirstReviewHours,copy))}</text>
    <text class="scope muted" x="382" y="399" text-anchor="end" style="font-size:9px">${escapeXml(copy.partialDay)}</text>
  </g>`;
  const card=(y,title,sub,chart)=>`<g><rect class="surface" x="20" y="${y}" width="380" height="250" rx="18"/>${header(title,sub,38,y+31)}${chart}${dateLabels(metrics.daily,cx,y+231,cw,6)}</g>`;
  return shell(w,h,copy,`<rect x="0" y="0" width="420" height="8" rx="4" fill="url(#accent)"/><text class="meta" x="20" y="32" fill="#4aa8ff" style="font-size:9.5px">${escapeXml(copy.context)}</text><text class="small faint" x="20" y="52">${escapeXml(copy.refreshed)} ${escapeXml(fmtGenerated(metrics.generatedAt,copy))}</text>${kpis(metrics,copy,true)}${flow}${card(ys[0],copy.dailyDelivery,copy.dailyDeliverySubtitle,groupedBars(metrics.daily,cx,ys[0]+74,cw,ph,copy))}${card(ys[1],copy.issueThroughput,copy.issueThroughputSubtitle,simpleBars(metrics.daily,cx,ys[1]+74,cw,ph,(d)=>d.issuesClosed,"#7867f2",copy.issuesClosed))}${card(ys[2],copy.codeMovement,copy.codeMovementSubtitle,churn(metrics.daily,cx,ys[2]+74,cw,ph,copy))}${card(ys[3],copy.pullRequestFlow,copy.pullRequestFlowSubtitle,leadBars(metrics.daily,cx,ys[3]+74,cw,ph,copy,metrics.flow.medianPrLeadTimeHours))}${footer}`);
}

export function renderRepositoryActivitySvg(metrics, locale, mobileView = false) {
  const copy = ACTIVITY_LOCALES[locale];
  if (!copy) throw new Error(`Unsupported repository-activity locale: ${locale}`);
  if (!metrics?.headline || !Array.isArray(metrics.daily) || metrics.daily.length !== WINDOW_DAYS) throw new Error("Invalid repository-activity metrics payload");
  return mobileView ? mobile(metrics,copy) : desktop(metrics,copy);
}
