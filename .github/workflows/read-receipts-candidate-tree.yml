name: read-receipts main source exporter

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: read-receipts-main-source-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  export:
    if: github.event.pull_request.head.ref == 'feat/read-receipts'
    runs-on: ubuntu-latest
    steps:
      - name: Check out exact main source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ba9bccc955507b04d0d29666b47ca6dfd121176a
          fetch-depth: 1
          persist-credentials: false

      - name: Export source archive
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "ba9bccc955507b04d0d29666b47ca6dfd121176a"
          archive="${RUNNER_TEMP}/towk-main-ba9bccc-source.tar.gz"
          status="${RUNNER_TEMP}/towk-main-source-status.txt"
          tar --exclude=.git --exclude=node_modules -czf "${archive}" .
          {
            printf 'main_sha=%s\n' "$(git rev-parse HEAD)"
            printf 'archive_sha256=%s\n' "$(sha256sum "${archive}" | cut -d' ' -f1)"
          } > "${status}"

      - name: Upload source archive
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: towk-read-receipts-main-source
          path: |
            ${{ runner.temp }}/towk-main-ba9bccc-source.tar.gz
            ${{ runner.temp }}/towk-main-source-status.txt
          retention-days: 1
          if-no-files-found: error
