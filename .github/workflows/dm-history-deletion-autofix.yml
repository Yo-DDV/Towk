name: DM history deletion autofix

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [main]

permissions:
  contents: write

concurrency:
  group: dm-history-deletion-autofix-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  format:
    if: github.event.pull_request.head.ref == 'feat/dm-history-deletion'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: feat/dm-history-deletion
          fetch-depth: 0
          persist-credentials: true
      - uses: ./.github/actions/setup
        with:
          install-frontend-deps: "false"
          mise-install-args: "go"
      - name: Format changed Go files and remove this workflow
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t files < <(git diff --name-only origin/main...HEAD -- '*.go')
          if ((${#files[@]})); then
            gofmt -w "${files[@]}"
          fi
          git rm .github/workflows/dm-history-deletion-autofix.yml
          git diff --check
      - name: Commit and push as Yoan
        shell: bash
        run: |
          set -euo pipefail
          git add --all
          test -n "$(git diff --cached --name-only)"
          export GIT_AUTHOR_NAME="$(git show -s --format=%an HEAD)"
          export GIT_AUTHOR_EMAIL="$(git show -s --format=%ae HEAD)"
          export GIT_COMMITTER_NAME="$(git show -s --format=%cn HEAD)"
          export GIT_COMMITTER_EMAIL="$(git show -s --format=%ce HEAD)"
          test "${GIT_AUTHOR_NAME}" = 'Yo-DDV'
          test "${GIT_COMMITTER_NAME}" = 'Yo-DDV'
          git commit -m 'style(dm): format private history deletion'
          test "$(git show -s --format=%an HEAD)" = 'Yo-DDV'
          test "$(git show -s --format=%cn HEAD)" = 'Yo-DDV'
          if git show -s --format=%B HEAD | grep -E 'Co-authored-by:|Generated-by:|Assisted-by:|Pair-programmed-by:'; then
            exit 1
          fi
          git push origin HEAD:feat/dm-history-deletion
