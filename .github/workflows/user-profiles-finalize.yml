name: user profiles final gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Final gate is temporarily paused while the reviewed patch chunks are reconstructed."
